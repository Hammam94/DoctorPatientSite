<%= form_for(@medicine_categroy) do |f| %>
  <% if @medicine_categroy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medicine_categroy.errors.count, "error") %> prohibited this medicine_categroy from being saved:</h2>

      <ul>
      <% @medicine_categroy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
